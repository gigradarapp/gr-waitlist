# Server-side only. Used by api/tally-count.js on Vercel.
# Do not prefix this with VITE_; VITE_* variables are exposed in the browser bundle.
#
# API key: log into Tally, open https://tally.so/settings/api-keys,
# create an API key, and store it as a Vercel environment variable.

TALLY_API_KEY=

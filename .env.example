# Copy to .env — exposed in the browser bundle.
#
# API key: log into Tally → https://tally.so/settings/api-keys → Create API key → copy once (starts with tly_…).
# Paste it below. If you change the embed form, update TALLY_EMBED_URL in src/tally.ts (form id is derived from that URL).

VITE_TALLY_API_KEY=
